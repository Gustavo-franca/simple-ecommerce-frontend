import { Dispatch } from 'react';
import ProductClient from '../../clients/product';
import { Product } from '../../types';

// const getProducts = () => {
//   return (dispatch: Dispatch<Product[]>) =>
//     new ProductClient({ config })
//       .getProducts({})
//       .then((data) => setProducts(data));
// };

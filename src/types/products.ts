export interface Product {
  imgURL: string;
  images: string[];
  id: string;
  name: string;
  description: string;
  rate: number;
  price: number;
  previousPrice: number;
  shippingPrice: number;
  stock: number;
}

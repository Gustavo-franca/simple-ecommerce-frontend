export interface Product {
  title: string;
  imgUrl: string;
  images: string[];
  id: string;
  description: string;
  rate: number;
  price: number;
  previousPrice: number;
  shippingPrice: number;
  stock: number;
}
